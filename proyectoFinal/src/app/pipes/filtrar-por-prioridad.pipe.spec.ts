import { FiltrarPorPrioridadPipe } from './filtrar-por-prioridad.pipe';

describe('FiltrarPorPrioridadPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltrarPorPrioridadPipe();
    expect(pipe).toBeTruthy();
  });
});
